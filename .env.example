


# Variables de entorno para el módulo de conductores
CONDUCTOR_JWT_SECRET="${CONDUCTOR_JWT_SECRET}"
CONDUCTOR_JWT_EXPIRES_IN="${CONDUCTOR_JWT_EXPIRES_IN}"
SMTP_HOST="${SMTP_HOST}"
SMTP_PORT="${SMTP_PORT}"
SMTP_USER="${SMTP_USER}"
SMTP_PASS="${SMTP_PASS}"




# Variables de entorno para el módulo de conductores
CONDUCTOR_JWT_SECRET="${CONDUCTOR_JWT_SECRET}"
CONDUCTOR_JWT_EXPIRES_IN="${CONDUCTOR_JWT_EXPIRES_IN}"
SMTP_HOST="${SMTP_HOST}"
SMTP_PORT="${SMTP_PORT}"
SMTP_USER="${SMTP_USER}"
SMTP_PASS="${SMTP_PASS}"




# Variables de entorno para el módulo de conductores
CONDUCTOR_JWT_SECRET="${CONDUCTOR_JWT_SECRET}"
CONDUCTOR_JWT_EXPIRES_IN="${CONDUCTOR_JWT_EXPIRES_IN}"
SMTP_HOST="${SMTP_HOST}"
SMTP_PORT="${SMTP_PORT}"
SMTP_USER="${SMTP_USER}"
SMTP_PASS="${SMTP_PASS}"

